# 独立工作区渲染验收（2026-09-23）

本次将现有 `zmh_v1` 分支同步到已验证的最新主分支代码，并记录部署验收；不改变 main，不改写分支历史。

## 配套代码与环境

- Adapter 代码：`dcd2d945eb04258dddae7d064dce2769b4e12149`。
- Server 代码：`517dfc3a222214de622da19c1d9c82355bb5316f`。
- Windows x64、UE 5.6、Python 3.12.13，两个仓库同级且独立克隆；无旧工作区 Git 对象或 Python 包依赖。
- 本次新增文档不改变上述已验证运行代码。详细系统验收见配套 server 的 `docs/WORKSPACE_RELEASE_20260923.md`。

## 验收证据

- Git LFS 对象完整性检查通过。
- Python：87 passed，19 subtests passed。
- UE Editor Win64 Development：26 个构建任务，成功。
- Ruff、CI 失败保护、Python/UE/兼容包版本一致性检查通过。
- 实机 UE 接收仿真数据，场景时间推进，全景与腕部相机可见。
- 8 秒权威采集保存 240 条样本与两路 MP4，数据集完成。
- 浏览器 WebRTC：1280×720，约 2.996 秒新增 245 个解码帧。仅为服务器本机观测，不保证异地网络帧率。

## 资源与提交边界

准备流程会重建部分材质、网格清单和带本机导入信息的 `.uasset`，这些本地生成差异没有作为源码修改提交。原始资源、生成器和构建脚本仍由 Git/Git LFS 管理；新克隆应运行现有准备流程，而不是依赖旧目录中的缓存。

不提交部署密钥、证书、账号库、采集数据、机器路径、虚拟环境、Saved、Intermediate 或 Binaries。此验收不包含长时间机械臂运动稳定性或异地公网压力测试。
